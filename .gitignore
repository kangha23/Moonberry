node_modules/
dist/
coverage/
playwright-report/
test-results/
.vercel/
.env
.env.*
!.env.example
.DS_Store
npm-debug.log*
.yarn-debug.log*
.yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo
# Hand-drawn override art (e.g. Sprout Lands free = non-commercial, no redistribution)
public/assets/override/*.png
public/assets/override/*.jpg

# Python bytecode from the ad-hoc image tooling
__pycache__/
*.pyc

# Subagent-driven-development scratch: ledger, briefs, reports, review packages.
# Git history is the durable record; this directory is working memory.
.superpowers/

# Downloaded upstream art packs. art/sources.json pins each file by sha256, so
# this folder is a cache rather than a record; the whole packs are not
# redistributed from this repo.
art/sources/
